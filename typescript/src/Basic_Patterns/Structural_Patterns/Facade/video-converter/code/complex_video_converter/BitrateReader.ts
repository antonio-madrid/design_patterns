import {Buffer} from "src/Basic_Patterns/Structural_Patterns/Facade/video-converter/code/complex_video_converter/Buffer";

class BitrateReader {
    public static read(filename: string, sourceCodec: any): Buffer {
        return Buffer.from([]);
    }

    public convert(buffer: Buffer, destinationCodec: any): Buffer {
        return Buffer.from([]);
    }
}

export { BitrateReader };